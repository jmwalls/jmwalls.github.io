{
  "name": "jmwalls.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "dev:mix": "mix watch",
    "dev:eleventy": "eleventy --serve",
    "dev": "npm-run-all clean -p dev:mix dev:eleventy",
    "build:mix": "mix --production",
    "build:eleventy": "eleventy",
    "build": "npm-run-all -s clean build:mix build:eleventy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0",
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.2",
    "laravel-mix": "^6.0.49"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "resolve-url-loader": "^5.0.0",
    "sass": "^1.69.7",
    "sass-loader": "^12.6.0"
  }
}
